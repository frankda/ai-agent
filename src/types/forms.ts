export interface FormField {
  displayName: string;
  label: string;
  placeholder: string;
  name: string;
  id: string;
  ariaLabel: string;
  autocomplete: string;
  tag: string;
  type: string;
  editable: boolean;
  sensitive: boolean;
}
